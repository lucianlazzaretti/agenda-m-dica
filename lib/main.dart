import 'package:flutter/material.dart';
import 'package:agendamento/views/home_page.dart';

void main() {
  //runApp(MyApp());

    runApp(MaterialApp(
      debugShowCheckedModeBanner: false,
      home: new HomePage(),
            theme: ThemeData(primarySwatch: Colors.teal)));
      }
